---
import { cn } from "#/lib/utils.ts";

type Props = {
	class?: string;
};

const { class: className } = Astro.props;
---

<div class={cn(["container mx-auto max-w-xl", className])}>
	<slot />
</div>
