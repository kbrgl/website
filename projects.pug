---
layout: layouts/page
eleventyExcludeFromCollections: true
---

h1(class="text-4xl mt-12 font-title font-medium dark:text-white") Projects
div(class="mt-8 grid gap-8 max-w-md")
    each project in projects
        section(class="space-y-0.5")
            a(href=project.link)
                h2(class="font-semibold text-lg text-blue-800 dark:text-blue-300 hover:text-amber-500 hover:dark:text-yellow-500 transition-colors") #{project.name} #[span(class="font-sans text-gray-400 dark:text-zinc-500 text-sm pl-2 font-normal")= project.date.split(' ')[2]]
            p(class="text-gray-500 dark:text-zinc-400")= project.description
