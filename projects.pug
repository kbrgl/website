---
layout: layouts/page
eleventyExcludeFromCollections: true
---

div(class="md:mx-24")
    h1(class="text-4xl mt-24 font-title font-black") Projects
    div(class="mt-8 grid gap-6")
        each project in projects
            section(class="flex flex-col md:flex-row bg-white dark:bg-zinc-800 border p-6 rounded-xl relative space-x-0 space-y-4 md:space-y-0 md:space-x-6 md:items-center max-w-screen-sm")
                a(href=project.link)
                    img(src=`/projects/${project.image}` alt="" class="rounded-xl max-w-[120px]")
                article(class="space-y-0.5 flex-1")
                    a(href=project.link)
                        h2(class="font-title font-bold text-lg hover:text-amber-500 hover:dark:text-yellow-500 transition-colors") #{project.name} #[span(class="font-sans text-gray-400 dark:text-zinc-500 text-sm pl-2 font-normal")= project.date.split(' ')[2]]
                    p(class="text-gray-500 dark:text-zinc-400")= project.description